import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    aqiContainer: {
      marginTop: 20,
    },
    aqiHeading: {
      fontSize: 18,
      fontWeight: 'bold',
      marginBottom: 10,
    },
  });

  export default styles